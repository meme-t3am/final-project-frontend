// import { useContext } from 'react';
// import { MemeContext } from '../Context/MemeContext';
// import { postToApi } from '../Services/meme.js';

export default function useMemes() {
  //   const { meme, setMeme } = useContext(MemeContext);

  return (
    <div>
      {/* 
    Functions:
    1. posting to user uploaded URL to API
    2. getting all memes from backend
    3. inserting chosen meme to table
     */}
    </div>
  );
}
